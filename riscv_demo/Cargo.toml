[package]
name = "riscv_demo"
version = "0.1.0"
edition = "2024"
autobins = false
autotests = false
autobenches = false

[features]
baseline = []

[dependencies]
riscv-rt = "0.13"

ed25519_heapless = { path = "../ed25519", default-features = false, features = [
    "fixed-bigint",
    "fixed-bigint-u8",
    "sha512-hmac-sha512",
] }
fixed-bigint = { version = "0.4", default-features = false, features = ["use-unsafe", "cios"] }
const-num-traits = { version = "0.1", default-features = false }

[profile.release]
codegen-units = 1
debug = true
lto = true
opt-level = "z"
